query GetDealersGraph($dealerId: Float!) {
  dealersGraph(dealerId: $dealerId) {
    nodes {
      id
      name
      type
      color
      adjustment
    }
    links {
      source
      target
    }
  }
}
